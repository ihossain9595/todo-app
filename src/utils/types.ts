export type EmployeeType = {
  name: string;
  employeeId: string;
  designation: string;
  email: string;
  phone: string;
};

export type TaskType = {
  id: string;
  employeeId: string;
  task: string;
};
